{
    "paltform": "laravel",
    "app": "hitour-backend",
    "args": [ "php artisan octane:start --server=swoole --workers=2 --task-workers=2 --host=0.0.0.0 --port=80" ],
    "disks": [
        {
            "name": "storage",
            "mountTo": "storage"
        },
        {
            "name": "public",
            "mountTo": "public"
        }
    ]
}